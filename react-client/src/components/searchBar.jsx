import React from 'react';

const SearchBar = (props) => (
  <div>
    <input type="text" defaultValue="Enter Location Here" id="search"></input>
    <button type="button" id='searchButton'>
    </button>
  </div>
)

export default SearchBar;